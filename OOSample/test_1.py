import question as q

def test_1():
	assert q.Run() == "Istanbul => (BU, University), (ITU, University), (DSI, HighSchool, German), (GS, HighSchool, French); Konya => (Selcuk, University), (KAL, HighSchool, English)", "Returned value is wrong"

